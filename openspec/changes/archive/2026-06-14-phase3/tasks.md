## 1. Fixture Inventory

- [x] 1.1 Add a dedicated `examples/callouts/callouts.html` fixture that demonstrates the reusable callout component, including a risk state
- [x] 1.2 Update `examples/README.md` so the canonical fixture inventory includes the shell, all six families, and the callout page
- [x] 1.3 Confirm every fixture references `../../infographic.css` with the correct relative path from its directory

## 2. Canonical Markup

- [x] 2.1 Normalize `examples/shell/shell.html` to the canonical page-shell contract
- [x] 2.2 Review and align each family fixture with the approved class contract and representative content
- [x] 2.3 Ensure the callout fixture uses only approved reusable callout states and shows the risk variant clearly

## 3. Verification

- [ ] 3.1 Open each fixture directly in a browser and confirm the page is readable without build tooling
- [x] 3.2 Verify that no fixture uses JavaScript, external fonts, or CDN assets
- [x] 3.3 Cross-check `examples/README.md` and the example files against the spec to confirm the fixture set matches the documented contract
